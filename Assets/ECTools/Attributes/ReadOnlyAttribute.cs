using UnityEngine;

namespace ECTools
{
    /// <summary>
    /// Shows a non interactable object on the inspector. The object must be serialized
    /// </summary>
    public class ReadOnlyAttribute : PropertyAttribute { }
}